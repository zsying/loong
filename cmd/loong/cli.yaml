# loong CLI 自身的组件树。命令组件全部 lazy：装配只登记，
# main 按 os.Args[1] 激活对应的命令组件执行。
type: cli
config:
  name: loong
children:
  - type: cli.list
    id: list
    lazy: true
  - type: cli.new
    id: new
    lazy: true
  - type: cli.tree
    id: tree
    lazy: true
